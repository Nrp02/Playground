# Print the first 15 numbers of the Fibonacci sequence using an
# iterative while loop (no recursion needed in this language subset).

let n = 15;
let a = 0;
let b = 1;
let i = 0;

while (i < n) {
    print a;
    let next = a + b;
    a = b;
    b = next;
    i = i + 1;
}
